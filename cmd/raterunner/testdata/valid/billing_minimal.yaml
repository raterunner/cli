# Test case: Minimal valid billing configuration with provider
# Expects: validation passes
version: 1
providers:
  - stripe
plans:
  - id: free
    name: Free Plan
    prices:
      monthly: { amount: 0 }
